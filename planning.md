# План реализации сервиса пополнения склада

## Назначение

Создать за пять часов веб-приложение для менеджера закупа Электрокомплекта: загрузить Excel, получить рекомендуемые заказы поставщикам, понять причины количества, изменить черновик и экспортировать результат. Команда — три студента, впервые участвующие в хакатоне. Рабочая среда — Codex desktop.

Это живой план по правилам `.agent/PLANS.md`. Разделы Progress, Surprises & Discoveries, Decision Log и Outcomes & Retrospective обновляет ответственный за интеграцию. Текущее поручение пользователя — подготовка проекта, а не реализация приложения.

## Progress

- [x] 2026-09-23: прочитан кейс целиком; проанализированы 12 книг Excel и 14 листов в двух архивах.
- [x] 2026-09-23: прочитаны обе страницы инструкции участников; подтверждены требования к README, GitHub и сдаче через платформу.
- [x] 2026-09-23: пользователь выбрал React и Python API, затем подтвердил Codex desktop и русский язык общения.
- [x] 2026-09-23: пользователь клонировал настоящий репозиторий; origin и чистая main проверены.
- [x] 2026-09-23: контекст, ограничения, план и роли записаны в репозиторий.
- [x] 2026-09-23: подготовительные документы сохранены локальным коммитом; созданы codex/api-integration, codex/data-engine и codex/web-ui. Отправки на GitHub не было.
- [ ] Перейти к задаче внутри проекта, сохранив этот контекст.
- [ ] Получить явное указание пользователя начать реализацию.
- [ ] Создать общий каркас, контракт API, синтетические примеры и окружение.
- [ ] Реализовать полный сценарий Systeme Electric, затем импорт IEK.
- [ ] Реализовать пять обязательных возможностей и проверки.
- [ ] Проверить интерфейс, корректировки, согласование и экспорт.
- [ ] Подготовить фактический README и демонстрацию; передать итог в репозиторий и на платформу по указанию команды.

## Обязательное поведение

Расчёт учитывает историю продаж, категории, доступные остатки, товар в пути, сезонность и устойчивый рост. Зафиксированные интервалы stockout — периоды отсутствия товара — повышают оценку спроса относительно наблюдаемых продаж. Единичные крупные покупки исключаются из регулярной потребности, но повторяющийся спрос сохраняется. Результат группируется по поставщику и содержит объяснение каждой строки.

Три экрана: «Данные и настройки», «Рекомендации», «Проверка заказа». Последний экран сохраняет исходную рекомендацию, правку менеджера и её причину, затем экспортирует CSV/XLSX. Автоматическая отправка поставщикам не входит в продукт.

## Технический подход

Frontend: React, TypeScript, Vite, Tailwind CSS и Recharts. Backend: Python 3.12, FastAPI, Uvicorn, Pydantic, pandas, NumPy и openpyxl. Хранилище: SQLite через стандартный sqlite3. Тестирование: pytest, HTTPX и Playwright. Node.js 22.12 или новее; совместимые версии зависимостей фиксируются один раз в общем каркасе.

Во время разработки frontend обращается к API. Для демонстрации собранный frontend обслуживается backend, чтобы приложение открывалось по одному адресу. Без отдельных микросервисов, Redis, облачной базы, авторизации и живой интеграции с 1С в первой версии.

Импорт, расчёт и экспорт — отдельные модули. Математика реализуется чистыми функциями, независимо от HTTP и базы. Преобразованные записи и расчёты сохраняются в SQLite; исходные файлы остаются вне Git. В каждом worktree своя база и зависимости.

## Контракты и поток данных

В первые 25 минут реализации ответственный за интеграцию согласует с исполнителями схемы Dataset, CalculationSettings, Recommendation, ItemExplanation и ManagerOverride. После этого схемы меняет только он. Набор обязательных полей рекомендации: поставщик, SKU, артикул, единица, категория, дата остатков, оценка спроса, доступный остаток, подходящие поставки, потребность до округления, рекомендуемое количество, срочность, объяснение и проблемы качества данных.

Основные интерфейсы: POST /api/datasets для загрузки; POST /api/calculations для запуска; GET /api/calculations/{id} для результата; GET /api/calculations/{id}/items/{item_id} для объяснения; PATCH этого item-адреса для правки количества; POST /api/calculations/{id}/review для проверки черновика; GET /api/calculations/{id}/export для экспорта. Экспорт использует тот же сохранённый результат и правки, что интерфейс. Нулевая правка отличается от отсутствующей.

Синтетические ответы по контракту позволяют frontend развиваться параллельно. Источники загружаются, распознаются по структуре, валидируются, приводятся к общей модели, рассчитываются и отображаются. Ошибки отдельных строк видимы; они не должны молча исчезать или превращаться в нули.

## Методика

Транзакции с 2025 года — основной источник недавнего спроса. Месячные отчёты используются для более ранней истории, сверки и помеченного fallback при отсутствии подробных данных. Одни продажи не учитываются дважды. Неполный сентябрь не считается полным месяцем.

Строки накладной агрегируются по товару и документу. Кандидаты в разовые всплески определяются устойчивым порогом на основе медианы и межквартильного размаха. При достаточной истории проверяется повторяемость крупных покупок по неделям; устойчивый рост не удаляется как аномалия. При нехватке истории требуется проверка менеджера. Исключённые наблюдения остаются в истории с причиной. При наличии анонимного customer_id дополнительно анализируются покупки клиента; для предоставленных файлов эта возможность проверяется отдельно на синтетике.

По явным интервалам stockout восстанавливается ожидаемый спрос на основе очищенных периодов наличия и сезонности. Восстановленная оценка корректирует скорость спроса и не добавляется повторно как невыполненный заказ. Месячные остатки дают только признак возможного отсутствия товара, не точную длительность.

Коэффициенты сезонности нормируются к среднему один. Базовый спрос оценивается после удаления сезонного эффекта, затем сезонность применяется один раз к прогнозному периоду. Рост определяется по нескольким полным месяцам; импортированный или ручной прогноз роста является альтернативой вычисленному, а не дополнительным множителем того же эффекта. Параметры буфера могут задаваться по категории, без выдумывания значения кодов категорий.

Потребность = прогноз на срок поставки плюс интервал пересмотра + страховой запас − доступный остаток − поставки, прибывающие внутри этого горизонта. Отрицательный результат становится нулём. Затем применяются подтверждённые ограничения поставщика. Даты будущих поставок отдельно используются для проверки нехватки до их прибытия.

Начальные демонстрационные допущения: дата 22.09.2026, срок поставки 14 дней, интервал пересмотра 7 дней, страховой запас 7 дней. Все параметры явно помечены как допущения и редактируемы. Для Systeme Electric исходный показатель наличия — «Свободный остаток»; соседние складские колонки без подтверждения не суммировать. Устаревшие остатки IEK и неизвестные преобразования единиц требуют явного решения менеджера.

## Этапы и критерии готовности

0:00–0:25: единый каркас, зафиксированный контракт, примеры, зависимости и команды запуска. Все исполнители получают один общий commit, после чего работают независимо. Конкретные команды проверок документируются в README после появления реализации.

0:25–1:15: импорт Systeme Electric, базовый расчёт и таблица в интерфейсе. Первый интеграционный результат — реальные загруженные данные превращаются в видимые рекомендации.

1:15–2:15: обязательные расчётные возможности, датированные поставки и импорт IEK. Изменение входных данных отражается в результате, ограничения показываются.

2:15–3:15: объяснения, графики, фильтры, правки, проверка и экспорт. Полный пользовательский сценарий проходит в браузере.

3:15–4:00: тесты расчётов, API, полного объёма данных и браузера; исправление существенных ошибок. Измерить время импорта и пересчёта на демонстрационном компьютере.

4:00–4:30: остановить добавление функций, написать русский README по фактическому состоянию, проверить запуск с нуля, подготовить трёхминутную демонстрацию.

4:30–5:00: репетиция, резервная запись, итоговая отправка кода в репозиторий команды и сдача через кнопку «Сдать решение». Публичный хостинг — только по отдельному решению; инструкция участников не делает его обязательным.

## Проверки и восстановление

При спросе 10 единиц в день, сроке 14 дней, пересмотре 7 дней, страховом запасе 7 дней, остатке 100 и подходящих поставках 40 потребность равна 140; при подтверждённой кратности 12 заказ равен 144. Больше подходящих поставок не увеличивает заказ; перенос поставки за горизонт убирает её вклад. Поздняя поставка не скрывает ранний дефицит.

Искусственный единичный всплеск не должен существенно увеличивать регулярный заказ. Повторяющиеся крупные покупки и рост сохраняются. Сезонный коэффициент меняет прогноз. Явный stockout повышает оценку спроса без двойного учёта. Проверить три даты поставки из комментариев Systeme Electric, нулевую правку, совпадение экспорта с интерфейсом, отсутствующие значения, холодный старт и несовпадение единиц.

Не менять оригиналы файлов. Повторный импорт не добавляет дубли незаметно. Не выполнять необратимые операции Git. Сохранять небольшие работающие commits; ошибочную новую правку откатывать отдельно с сохранением чужой работы. Не заявлять точность прогноза или экономию без измерений. После обязательных тестов допускается небольшой тест прогноза на отложенном временном интервале.

## Surprises & Discoveries

Новая ZIP-копия репозитория содержит только README; пользователь затем создал настоящий clone. Данные достаточно объёмны для демонстрации, но входы, перечисленные в кейсе, представлены не полностью. Особенности 12 книг, скрытых листов, комментариев и ошибок формул записаны в `docs/DATA_NOTES.md`.

## Decision Log

2026-09-23: выбран веб-продукт на React и Python API, а не устанавливаемое приложение: это соответствует работе закупщика и пятичасовому сроку. Пользователь подтвердил Codex desktop и русский язык. Сначала реализуется Systeme Electric из-за более полных актуальных данных. Только один интегратор меняет контракт и объединяет ветки. Расчёт не зависит от LLM. Приватные данные не публикуются.

## Outcomes & Retrospective

Выполнены исследование, подготовка документации и создание трёх локальных веток от общего коммита с контекстом. Изменения пока только на компьютере капитана. Главная ветка дополнительно содержит эту запись о подготовке; перед параллельной реализацией рабочие ветки должны получить общий каркас из main. Приложение, тесты, API и интерфейс ещё не созданы. Не представлять перечисленные функции как реализованные. Дальнейшая реализация начинается после явного указания пользователя.

История изменений: 2026-09-23 — перенос согласованного плана и контекста из отдельного разговора в реальный репозиторий команды.
