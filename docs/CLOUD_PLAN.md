# Бесплатная публичная демонстрация

Этот ExecPlan поддерживается по .agent/PLANS.md. Цель — один HTTPS-адрес для комиссии с сохранением согласованных заказов после рестарта. Локальная SQLite остаётся резервом, данные партнёра не публикуются.

## Progress

- [x] Пользователь создал Render/Neon и выбрал ввод секрета самостоятельно.
- [x] PostgreSQL-адаптер с блокировкой строки и тем же интерфейсом Repository.
- [x] Публичный режим блокирует импорт и чтение несинтетических данных.
- [x] Dockerfile, render.yaml с явным free и инструкция docs/DEPLOYMENT.md.
- [x] 53 теста с настоящей локальной PostgreSQL, включая конкуренцию, ноль, версии, повторный seed и публичные ограничения.
- [x] Контейнер: HTTP-сценарий настоящего движка, ноль, согласование, CSV и сохранение после рестарта прошли.
- [x] Frontend f469376 объединён; публичное демо отличает настоящий расчёт от фиксированного примера.
- [x] Main cf4e96e опубликована; PostgreSQL и 13 сценариев нового frontend проверены.
- [x] Render заблокирован разрешением организации. Резерв Cloudflare Tunnel опубликован, внешний HTTP-сценарий прошёл.
- [ ] Постоянный хостинг без зависимости от ноутбука: дождаться разрешения организации для Render.

## Контекст и порядок действий

Settings читает DATABASE_URL только из окружения. Service выбирает app/storage/postgres.py или прежнюю SQLite. PostgreSQL хранит JSON в таблицах objects и order_versions; SELECT FOR UPDATE блокирует строку заказа до окончания проверки revision и записи. Старые базы не удаляются. Публичный режим требует постоянную базу и включённое демо.

Docker собирает React Node 24, затем запускает Python 3.12 от непривилегированного пользователя. app/cloud.py повторяемо добавляет фиксированное демо и наблюдения app/engine_demo.py для настоящего статистического расчёта. Ни источник, ни Docker-контекст не включают оригиналы партнёра.

Из корня выполнить docker build -t nomad-coders:demo .; запустить с DATABASE_URL, NOMAD_DEMO=1, NOMAD_PUBLIC_DEMO=1. Локальная тестовая PostgreSQL доступна на 127.0.0.1:55432, внутри Docker через host.docker.internal:55432. Тесты backend/tests/test_postgres.py читают NOMAD_TEST_DATABASE_URL и создают только собственные одноразовые схемы.

После проверки опубликовать main. Капитан создаёт Free Web Service по docs/DEPLOYMENT.md и вводит секрет из Neon непосредственно в Render. Публичный адрес — вход для внешней проверки; до его получения не объявлять сайт опубликованным.

## Проверка и приёмка

На реальном HTTP открыть главную и вложенный маршрут, получить JSON-404 для /api/unknown. Вычислить synthetic statistical-v1, сохранить заказ с нулевой правкой и согласовать. Скачать CSV, перезапустить контейнер/сервис и получить тот же заказ. Импорт XLSX возвращает 403. Повторный старт не создаёт дубль набора. На HTTPS повторить браузерный сценарий и отметить фактический холодный старт без обещания 100% доступности.

## Surprises & Discoveries

Docker Desktop был установлен, но не запущен; запущен для Linux-проверки. Исключения .dockerignore уточнены: одного разрешения каталога недостаточно для исключения его node_modules; итоговый контекст составляет десятки килобайт вместо 146 МБ. Секрет не выводится repr(Settings).

## Decision Log

23.09.2026: из-за срока менее часа оставляем один web-сервис и внешнюю бесплатную БД, без авторизации, очередей и платного диска. Публичные заказы общие, данные исключительно синтетические. Настоящий импорт доступен локально. Исходный пользовательский ввод секретов предпочтительнее передачи их в чат.

## Outcomes & Retrospective

Код, PostgreSQL и Linux-контейнер проверены. Заказ db64ca79-2ed3-4bf9-9f55-e80c9faad071 пережил рестарт тестового контейнера. Бесплатный HTTPS-сервис пока не создан: приватный репозиторий требует подключения Render через GitHub, возможно одобрение организации. Успешная локальная сборка не означает публикацию. При ошибке конфигурации исправляется env, базы и заказы не сбрасываются.

23.09.2026: из-за блокировки Render и срока менее 45 минут запущен резервный Cloudflare Tunnel только на синтетический контейнер 8011. Ссылка работает, но зависит от ноутбука; пользователь предупреждён. Это не замена постоянному облачному хостингу.

Обновлено 23.09.2026: публичный HTTP-сценарий пройден; ссылка и ограничения добавлены в README.
